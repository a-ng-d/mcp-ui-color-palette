{
  "name": "mcp-ui-color-palette",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.1",
    "prettier": "^3.5.3",
    "typescript": "^5.0.0",
    "wrangler": "^3.90.0"
  }
}
