{
  "name": "mcp-ui-color-palette",
  "version": "1.0.4",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "start:8585": "wrangler dev --port 8585",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.1",
    "prettier": "^3.5.3",
    "typescript": "^5.0.0",
    "wrangler": "^4.83.0"
  },
  "dependencies": {
    "agents": "^0.11.2",
    "zod": "^4.3.6"
  }
}
